export const IMAGE_UPLOAD_CONFIGS = {
  resolution: 1080, // the target resolution
  scaleMayor: true, // scale the mayor side of the image or the minor side
  maxQuality: 0.9, // max jpeg compression alowed
  minQuality: 0.7, // min jpeg compression alowed
  maxSize: 1000, // max size alowed in KB
  targetSize: 500, // the preferred size in KB
};
